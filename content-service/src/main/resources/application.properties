spring.application.name=content-service
server.port=8081

# --- CONFIGURARE BAZA DE DATE (Hardcodata pentru testare locala) ---
spring.datasource.url=jdbc:postgresql://localhost:5432/content_database
spring.datasource.username=postgres
spring.datasource.password=kuso
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Aceasta linie poate fi stearsa (Hibernate 6 detecteaza singur dialectul), dar nu strica
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect